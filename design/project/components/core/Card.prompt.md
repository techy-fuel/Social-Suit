The base surface for every panel in SocialSuite — stat cards, chart panels, table wrappers, settings sections. Never add a colored left-border accent.

```jsx
<Card title="Growth" action={<PeriodSelect />}>
  <LineChart data={data} />
</Card>
```
