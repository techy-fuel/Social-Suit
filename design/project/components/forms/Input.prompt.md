Text field used across Composer, Hashtag Tracker's start-session form, and Settings.

```jsx
<Input label="Hashtag" placeholder="#HIRAOpenDay" value={tag} onChange={e => setTag(e.target.value)} />
<Input label="Caption" error="Exceeds Instagram's 2,200-character limit by 84 characters" />
```
