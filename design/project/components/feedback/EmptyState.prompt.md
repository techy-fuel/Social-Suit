Zero-state panel — no connections yet, no scheduled posts this week, no tracking sessions.

```jsx
<EmptyState icon={<i data-lucide="link-2" />} title="Connect a platform to start scheduling"
  description="HIRA Institute has no connected accounts yet." action={<Button>Add connection</Button>} />
```
