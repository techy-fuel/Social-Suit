Transient system notification — post published, report generated, connection dropped.

```jsx
<Toast tone="positive" title="Report ready to send" description="HIRA Institute — June performance" />
<Toast tone="error" title="3 accounts disconnected" description="Reconnect to resume scheduling" />
```

The single left accent border here (not on Card) is intentional — toasts are transient overlays, not persistent surfaces.
