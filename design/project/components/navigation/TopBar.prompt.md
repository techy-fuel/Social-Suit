Fixed white top bar framing every screen — page title left, WorkspaceSwitcher always top-right.

```jsx
<TopBar title="Page overview" workspace={<WorkspaceSwitcher workspace={{initials:'HI',name:'HIRA Institute'}} />} />
```
